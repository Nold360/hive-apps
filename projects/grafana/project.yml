config:
  description: Grafana, Prometheus and friends

apps:
- name: kube-prometheus-stack
  repoURL: https://prometheus-community.github.io/helm-charts 
  chart: kube-prometheus-stack
  targetRevision: 34.9.1
  secrets:
    - name: grafana
      keys:
        - admin-password
        - admin-user
