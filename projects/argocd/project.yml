config:
  description: ArgoCD - Continous Deployment from Git

apps:
- name: argocd
  repoURL: https://argoproj.github.io/argo-helm
  chart: argo-cd
  targetRevision: 5.13.6
  syncWave: '0'
  syncPolicy:
    automated:
      prune: false
  secrets:
  - name: argocd-secret
    keys:
    - admin.password
    - server.secretkey
    - oidc.auth0.clientSecret
    - webhook.gogs.secret
  - name: ca-cert
    keys:
    - ca
